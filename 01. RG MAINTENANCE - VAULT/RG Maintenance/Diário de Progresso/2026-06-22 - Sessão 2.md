---
data: 2026-06-22
sessao: 2
fase_ativa: "Fase 2 — Checklist/Intervenções + Histórico + bump segurança"
estado: "🟢 Build verde (tsc + build). Crítica de segurança resolvida. Gate A1 pendente do Rui"
tags: [diario, sessao, rg-maintenance, app, firebase, intervencoes, historico, seguranca]
---

# 📝 Sessão 2 — 2026-06-22 — Checklist/Intervenções + Histórico + bump Next

[[../00 - Projeto Overview|← Overview RG Maintenance]]

## Contexto

Rui aprovou: (a) **bump de segurança do Next** e (b) **construir Checklist/Intervenções + Histórico**.

## Bump de segurança

- `package.json` — **Next 15.1.0 → 15.5.19** (+ `eslint-config-next` 15.5.19)
- `npm audit`: **crítica do Next resolvida** (era RCE/DoS/SSRF/XSS). Restam **10 moderadas**, todas
  transitivas do `firebase-admin` (`@google-cloud/storage` → `teeny-request`/`retry-request`/`uuid`),
  num caminho (Cloud Storage) **que não usamos** (só Firestore + Auth). `audit fix --force` partiria o
  Admin SDK → deixadas; risco real baixo.

## Checklist / Intervenções

- `src/lib/firebase/data.ts` — funções de intervenções: `listInterventions`, `listInterventionsByTask`,
  `createIntervention`, `deleteIntervention`
- `src/app/dashboard/tasks/[id]/page.tsx` — página de **detalhe da tarefa** (resumo + equipamento +
  lista de intervenções)
- `src/app/dashboard/tasks/[id]/TaskDetailClient.tsx` — formulário de intervenção: **checklist dinâmica**
  (adicionar/remover/marcar pontos), técnico, início/fim, observações, opção "marcar tarefa concluída"
- `src/app/dashboard/tasks/[id]/actions.ts` — `createInterventionAction` (checklist serializada em JSON;
  atualiza estado da tarefa se pedido) + `deleteInterventionAction`
- `src/app/dashboard/tasks/TasksClient.tsx` — título da tarefa agora **linka** para o detalhe

## Histórico

- `src/app/dashboard/history/page.tsx` — lista global de intervenções (tarefa, equipamento, técnico,
  data/duração, nº pontos checklist, observações); linka para a tarefa
- `src/components/layout/Sidebar.tsx` — novo item de navegação **Histórico**

## Verificação

- `npx tsc --noEmit` → ✅ 0 erros
- `npm run build` → ✅ verde (9 rotas, incl. `/dashboard/history` e `/dashboard/tasks/[id]`); Next 15.5.19

## ⚠️ Para o Rui (inalterado)

- **Gate A1:** criar projeto Firebase + `.env.local` + deploy Vercel (ver `SETUP.md`). App compila mas
  não corre sem credenciais.

## Próximos passos (Fase 2 cont.)

- Ecrã de **registo** (cria empresa + gestor) para dispensar criação manual no Firestore
- **Materiais** consumidos por intervenção (modelo já existe) · **Relatórios PDF** (SHOULD)
- **Utilizadores/permissões** (gestão de técnicos)
