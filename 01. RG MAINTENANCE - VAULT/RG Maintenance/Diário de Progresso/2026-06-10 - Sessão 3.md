---
data: 2026-06-10
sessao: 3
fase_ativa: Setup global Claude Code (transversal, não só RG Maintenance)
estado: "🟢 Restart + login feitos, Playwright MCP confirmado a funcionar — falta decisões do Rui (Tasks #4, #5, #7)"
tags: [diario, sessao, rg-maintenance, claude-code, setup-global]
---

# 📝 Sessão 3 — 2026-06-10 — Setup global do Claude Code

[[../00 - Projeto Overview|← Overview RG Maintenance]]

> ⚠️ Esta sessão não é só sobre RG Maintenance — é sobre o **ambiente Claude Code global**
> (`C:\Users\Quinta do Arrobe\.claude\`), que serve todos os projetos.
> Fica registada aqui por ser o único Diário de Progresso ativo até agora.

## Resumo da Sessão

**Participantes:** Rui Garrido + Claude (Claude Code, local)

Continuação da migração Cowork/claude.ai → Claude Code local. Sessão 2 tinha instalado globalmente o
`everything-claude-code` (agentes/skills/comandos/rules) mas sem ativar hooks nem ferramentas de browser.
Pedido do Rui nesta sessão: ativar tudo o que for seguro, instalar Playwright, ligar ao Chrome,
registar o que falta vs. o que está disponível, e documentar tudo.

## O que foi feito

### 1. Regras gerais do Claude Code
- [x] Criado `~/.claude/CLAUDE.md` — instrui o Claude a usar de forma **autónoma** os agentes/skills/comandos
  instalados (planner, architect, tdd-guide, code-reviewer, security-reviewer, build-error-resolver,
  e2e-runner, refactor-cleaner, doc-updater, etc.) sempre que o pedido encaixar, sem o Rui ter de os
  invocar explicitamente.

### 2. Hooks do everything-claude-code — ativados (subconjunto seguro)
- [x] Adicionado bloco `hooks` a `~/.claude/settings.json` com:
  - Aviso (não bloqueante) antes de `git push`
  - Log do URL do PR após `gh pr create`
  - Auto-format Prettier + verificação TypeScript após editar `.ts/.tsx/.js/.jsx`
  - Aviso de `console.log` esquecido (após edição e no fim de cada resposta, se houver repo git)
- [x] **NÃO ativados** (deliberado):
  - Bloqueio de `npm run dev` fora de `tmux` → não há tmux no Windows por defeito, bloquearia o dev server
  - Bloqueio de criação de `.md` fora de README/CLAUDE/AGENTS/CONTRIBUTING → bloquearia todo o trabalho
    no Vault Obsidian (este próprio ficheiro seria bloqueado)
  - Hooks de `memory-persistence`/`strategic-compact` (SessionStart/SessionEnd/PreCompact/suggest-compact)
    → scripts `.sh` (bash-only) com paths `${CLAUDE_PLUGIN_ROOT}` que não resolvem fora de um plugin
    instalado → ficou registado como tarefa para adaptar a Node/cross-platform (Task #2)

### 3. Playwright instalado
- [x] `npm install -g playwright @playwright/test` → `playwright@1.60.0`
- [x] `npx playwright install chromium` → browser Chromium descarregado

### 4. MCP Playwright (browser automation) configurado
- [x] Adicionado a `~/.claude.json` → `mcpServers.playwright`:
  - `npx -y @playwright/mcp@latest --browser chrome --user-data-dir ~/.claude/playwright-profile`
  - Usa o **Chrome instalado** (`C:\Program Files (x86)\Google\Chrome\Application\chrome.exe`, v149) com
    um **perfil persistente dedicado** — fica gravado entre sessões
- [x] Backup do `.claude.json` anterior: `.claude.json.before-playwright-2026-06-10T20-29-13-388Z`
- ⏳ **Requer reiniciar o Claude Code** para o MCP ficar disponível (servidores MCP só carregam no arranque)
- ⏳ Depois do restart: primeira vez que o browser abrir, **o Rui tem de fazer login manualmente**
  (claude.ai, Google, etc.) nesse perfil — fica guardado para sempre a partir daí

> Nota sobre `cachedChromeExtensionInstalled: true` em `~/.claude.json`: esta flag refere-se à extensão
> "Claude in Chrome" (Anthropic), que é um produto separado, controlado a partir do claude.ai/Claude
> Desktop — **não** é algo que o Claude Code (CLI) consiga "ligar-se" diretamente. O caminho real para
> dar ao Claude Code acesso ao browser é o **MCP Playwright** acima, que é o que foi configurado.

### 5. Tasks criadas (Claude Code TaskList)
Tudo o que falta / pode continuar ficou registado como tasks:

| # | Tarefa | Tipo |
|---|--------|------|
| 1 | Configurar e testar Playwright MCP (Chrome) | ✅ Posso continuar (após restart) |
| 2 | Adaptar hooks memory-persistence/strategic-compact p/ Windows | ✅ Posso continuar |
| 4 | MCPs pendentes de credenciais (Figma, Canva, Gmail, Drive, Google Tasks, Netlify) | ❌ Sem acesso — credenciais do Rui |
| 5 | RG Maintenance — Gate A1 (contas + deploy) | ❌ Sem acesso — contas do Rui |
| 6 | RG Maintenance — Migração Supabase→Firebase (Decisão #010) | ⏸️ Em espera (deliberado) |
| 7 | RG Maintenance — Decisão #003 (domínio/SaaS/técnicos) | ❌ Decisão do Rui |
| 8 | Verificar 1º check-in semanal automático (cron 067c2cf7, 2026-06-15) | ✅ Posso continuar |

### 6. Continuação (mesmo dia, depois do restart) — Playwright MCP testado + extensão "Claude in Chrome"

- [x] **Restart feito** — ferramentas `mcp__playwright__*` disponíveis na sessão.
- [x] **Login manual em claude.ai** — feito no perfil persistente do Playwright (garrido.rui@gmail.com,
  Plano Pro). MCP Playwright confirmado a funcionar (navegação, cliques, snapshots, etc.).
- [x] **Extensão "Claude in Chrome" instalada** (Chrome Web Store, v1.0.75) no perfil do Playwright,
  com OAuth autorizado para a mesma conta.
- ⚠️ **`/chrome` (CLI) não ligou** — comando devolveu vazio duas vezes. Conclusão (consistente com a nota
  da secção 4): a extensão "Claude in Chrome" liga-se via native messaging ao **Chrome normal do
  sistema**, não ao perfil isolado do Playwright. O Playwright MCP continua a ser o caminho real e já
  está a funcionar — a extensão não é necessária para o Claude Code aceder ao browser.
- ⚠️ Nota adicional: a FAQ da própria página claude.ai/chrome/installed diz que "Claude in Chrome" em
  beta está disponível só para **Plano Max** — a conta do Rui é Plano Pro, pelo que mesmo ligando no
  Chrome normal pode não funcionar plenamente.
- **Decisão**: não perseguir mais o `/chrome`/extensão por agora — não é bloqueador. Se o Rui quiser
  mesmo assim, teria de instalar a extensão no Chrome do dia-a-dia, login, e correr `/chrome` aí.

### 7. Dashboard de Status (transversal) — criado `G:\status-dashboard`

A pedido do Rui, criado um mini-site estático com 2 dashboards de estado (EneaSpirit + RG Maintenance),
para ter uma visão rápida do progresso de ambos os projetos sem abrir os Vaults.

- [x] **Estrutura criada em `G:\status-dashboard\`**:
  - `index.html` — página inicial com cartões-resumo dos dois projetos
  - `eneaspirit.html` + `eneaspirit.js` — fases (0-5), estado AG-07, biblioteca PDFs por TEMA,
    tarefas pendentes, monetização, atividade recente
  - `rg-maintenance.html` + `rg-maintenance.js` — fases, gates A0-A7b, decisões pendentes,
    tarefas em aberto, atividade recente, links rápidos
  - `style.css` + `app.js` (utilitários partilhados)
  - `data/eneaspirit.json` + `data/rg-maintenance.json` — dados de estado (a atualizar manualmente
    no fim de cada sessão relevante)
  - `netlify.toml` — configuração de publicação (raiz do site, headers de segurança básicos)
- [x] **Testado localmente** com `python -m http.server` + Playwright MCP (screenshots OK, sem erros
  de consola além de favicon em falta)
- [x] **Netlify CLI instalado** (`netlify-cli@26.1.0`)
- ⏳ **Falta**: `netlify login` (OAuth interativo — só o Rui pode autorizar) + 1º `netlify deploy --prod`
  para publicar o site (decidir nome do site/conta "eneaspirit" no Netlify)
- 📌 **Manutenção**: sempre que houver progresso relevante num dos projetos, atualizar o `data/*.json`
  correspondente e correr `netlify deploy --prod` a partir de `G:\status-dashboard`

## Ações Manuais Necessárias (Rui)

Para isto ficar 100% operacional:

1. ~~**Reiniciar o Claude Code**~~ — ✅ feito, MCP Playwright disponível e confirmado.
2. ~~**Login manual no Chrome do Playwright**~~ — ✅ feito, login em claude.ai (garrido.rui@gmail.com,
   Plano Pro) no perfil persistente do Playwright.
3. **Credenciais dos MCPs em falta (Task #4)** — quando quiseres ligar Figma/Canva/Gmail/Drive/Google
   Tasks/Netlify, preciso das respetivas API keys/tokens.
4. **RG Maintenance Gate A1 (Task #5)** — criar contas GitHub + Vercel (+ Firebase quando chegar a
   altura) e seguir `G:\rg-maintenance\SETUP.md`.
5. **Decisão #003** — responder às 3 perguntas de negócio (domínio, modelo SaaS/interno, nº técnicos)
   no [[../Decisões/Log de Decisões|Log de Decisões]].
6. **`netlify login`** (terminal, abre o browser para autorizar a conta Netlify) — necessário para
   publicar o `G:\status-dashboard` (dashboards EneaSpirit + RG Maintenance). Depois disto, posso correr
   `netlify deploy --prod` sozinho.

## Próximos Passos

1. ~~Após restart + login: testar o MCP Playwright~~ — ✅ feito e confirmado.
2. Adaptar hooks `memory-persistence`/`strategic-compact` para Windows/Node (Task #2) — posso continuar
   sem input do Rui, se for essa a prioridade agora.
3. Continuar Fase 1 do RG Maintenance assim que o Rui tiver as contas (Task #5)
4. Segunda-feira 2026-06-15: confirmar check-in semanal automático (Task #8)
5. Assim que o Rui fizer `netlify login`, publicar `G:\status-dashboard` com `netlify deploy --prod`

## Nota — limpeza de referências antigas

A pedido do Rui, removidas as menções a "FestWay"/"Octogent" desta nota e da Overview (a Task #3, que
era sobre isso, foi apagada). Não foi mexido em ficheiros internos do Claude Code (transcripts `.jsonl`,
`paste-cache`, `file-history` em `~/.claude/`) — são registos de sessão imutáveis, não documentos de
projeto, e ainda contêm o termo nesses históricos. Se for preciso limpar isso também, é preciso indicar
explicitamente da próxima vez (envolve apagar histórico de conversas).
