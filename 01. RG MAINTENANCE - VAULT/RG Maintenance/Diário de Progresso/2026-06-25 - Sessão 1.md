---
data: 2026-06-25
sessao: 1
fase_ativa: "Fase 2 — módulo Stocks + relatórios + exportações"
estado: "🟢 Build verde (22 rotas). Deploy Vercel OK."
tags: [diario, sessao, rg-maintenance, stocks, pdf, exportacao, intervencao]
---

# 📝 Sessão 1 — 2026-06-25 — Stocks, materiais na intervenção, relatório PDF, exportação histórico

[[../00 - Projeto Overview|← Overview RG Maintenance]]

## Checkpoint 08:40 — Módulo Stocks + Materiais na intervenção + Relatório PDF

Sessão autónoma (continuação após compactação de contexto anterior sobre RG Maintenance).

---

## Módulo de Gestão de Stocks

- `src/types/models.ts` — adicionado `StockItem` (id, companyId, name, reference, category, quantity, unit, unitCost, minQuantity, location, createdAt, updatedAt)
- `src/lib/plans.ts` — adicionada feature key `'stocks'` (disponível a partir do plano Starter); GATES e UpgradeModal actualizados
- `src/lib/firebase/data.ts` — funções `listStockItems`, `createStockItem`, `updateStockItem`, `deleteStockItem`, `decrementStockQuantity` (usa `FieldValue.increment`)
- `src/app/dashboard/stocks/page.tsx` — nova página (force-dynamic, gated por plan)
- `src/app/dashboard/stocks/StocksClient.tsx` — tabela de stocks com alerta "stock baixo" (quando qty ≤ minQuantity), CRUD completo (criar/editar/apagar) via modal
- `src/app/dashboard/stocks/actions.ts` — server actions `createStockItemAction`, `updateStockItemAction`, `deleteStockItemAction`
- `src/components/layout/Sidebar.tsx` — link "Stocks" com ícone `Boxes` adicionado ao nav de gestores

## Materiais dentro do modal de intervenção

- `src/app/dashboard/tasks/[id]/page.tsx` — passa `stockItems` como prop para `TaskDetailClient`
- `src/app/dashboard/tasks/[id]/TaskDetailClient.tsx` — reescrito para incluir:
  - Secção "Materiais utilizados" dentro do modal (antes só existia pós-criação)
  - Dropdown de stock com disponibilidade actual; preenche automaticamente nome/ref/unit/custo ao seleccionar
  - Suporte a materiais livres (sem stock)
  - Botão "Fechar OT" agora é secção destacada (borda verde, ativa ao assinalar; botão muda para "Guardar e Fechar OT")
- `src/app/dashboard/tasks/[id]/actions.ts` — `createInterventionAction` actualizado:
  - Lê `inlineMaterials` do formData (JSON array)
  - Cria um `Material` por cada item e chama `decrementStockQuantity` quando `stockItemId` presente
  - Adiciona `revalidatePath('/dashboard/stocks')`

## Relatório PDF por tarefa

- `src/app/report/layout.tsx` — layout mínimo fora do dashboard (sem sidebar)
- `src/app/report/task/[id]/page.tsx` — página standalone com CSS inline A4:
  - Cabeçalho (logo RG + empresa + data)
  - Dados da tarefa em grelha 4×2 (estado, equipamento, tipo, criticidade, responsável, prazo, criada, nº intervenções)
  - Regras de segurança (se existirem)
  - Cada intervenção: técnico, datas, duração, checklist, observações, contagem de fotos, tabela de materiais com totais
  - Resumo (nº intervenções, tempo total, custo materiais)
- `src/app/report/task/[id]/PrintTrigger.tsx` — dispara `window.print()` 700ms após carregar
- `src/app/report/task/[id]/PrintBar.tsx` — barra fixa (ecrã) com botões "Fechar" + "Imprimir / Guardar PDF"
- `src/app/dashboard/tasks/[id]/page.tsx` — botão "Relatório PDF" que abre `/report/task/${id}` em nova tab

## Exportação do Histórico

- `src/app/dashboard/history/HistoryExportButtons.tsx` — componente cliente com:
  - Botão "Imprimir" (`window.print()`) — histórico formatado para impressão
  - Dropdown "Exportar": CSV de intervenções (14 colunas) + CSV de materiais (9 colunas), ambos com BOM UTF-8 para Excel PT
- `src/app/dashboard/history/page.tsx` — inclui `HistoryExportButtons`, CSS `@media print` (oculta sidebar, mostra header da empresa), passa todos os dados como props

## Checkpoint 08:47 — Campos em falta na Nova Tarefa + Fix banner amarelo

### Campos dinâmicos na criação/edição de tarefas

Após screenshot do utilizador (modal "Nova tarefa" sem campos de segurança/materiais):

- `src/types/models.ts` — adicionado `materialsRequired?: string[] | null` ao interface `Task`
- `src/app/dashboard/tasks/actions.ts` — refactored `parseTask`: helper `parseStringArray(key)` interno, agora lê tanto `safetyRules` como `materialsRequired` do formData (JSON arrays)
- `src/app/dashboard/tasks/TasksClient.tsx` — reescrito com sub-componente `DynamicList` (lista dinâmica com add/remove por item); dois novos blocos no formulário: "Regras de segurança" (ShieldAlert) + "Materiais a utilizar" (Package); state + useEffect para sincronizar com `editing` task
- `src/app/dashboard/tasks/[id]/TaskSummaryActions.tsx` — mesmo tratamento para o modal de edição na página de detalhe: `DynamicList` adicionado para ambos os campos, `useEffect` sincroniza com `task.safetyRules`/`task.materialsRequired` ao abrir modal

### Fix banner amarelo

- `src/app/dashboard/layout.tsx` — removido `EmailVerifyBanner` e import `getSessionUser`; banner de verificação de email não faz sentido em CMMS B2B onde contas são criadas pelo gestor

## Checkpoint 09:xx — Fix redirect loop + renomear labels + calendário melhorado

### Fix ERR_TOO_MANY_REDIRECTS

- `src/app/api/auth/logout/route.ts` — novo route GET que apaga cookie `__session` e redireciona para `/login`; evita loop middleware ↔ dashboard quando cookie está expirado
- `src/app/dashboard/layout.tsx` — quando `getCurrentProfile()` retorna null, redireciona para `/api/auth/logout` (excluído do matcher middleware) em vez de `/login`

### Renomeação de labels (Intervenção → Tarefa)

- `src/app/dashboard/page.tsx` — "Nova Intervenção" → "Nova Tarefa"; KPI label "Intervenções" → "Tarefas"; header "ID Ordem" → "ID Tarefa"
- `src/app/dashboard/tasks/[id]/TaskDetailClient.tsx` — "Intervenções (X)" → "Registos (X)"; "Registar intervenção" → "Registar trabalho"; "Fechar Ordem de Trabalho" → "Fechar Tarefa"; "Guardar intervenção" → "Guardar registo"; "Guardar e Fechar OT" → "Guardar e Fechar Tarefa"; "Sem intervenções..." → "Sem registos..."
- `src/components/ui/UpgradeModal.tsx` — "Histórico de Intervenções" → "Histórico de Tarefas"

### Botão PDF só para tarefas concluídas

- `src/app/dashboard/tasks/[id]/page.tsx` — botão "Relatório PDF" condicional: só aparece quando `task.status === 'done'`

### Calendário — Nova Tarefa + Vista Semana

- `src/app/dashboard/calendar/CalendarClient.tsx` — reescrito com:
  - Toggle **Mês / Semana** no header do calendário
  - Vista semanal: grelha 7 colunas com mín. 140px por célula, eventos visíveis sem limite de 3
  - Botão **"Nova Tarefa"** no painel de dia selecionado (abre modal com data pré-preenchida)
  - Modal de criação de tarefa: título, descrição, tipo, criticidade, equipamento, responsável
  - `buildEventMap(tasks, plans, start, end)` — função genérica para range (funciona para mês e semana)
  - `computePlanOccurrencesInRange(plan, start, end)` — range genérico em vez de mês+ano

## Checkpoint — Fix modal "Nova Tarefa" no calendário

Problema: ao criar tarefa pelo calendário, o modal não mostrava os campos de "Regras de segurança" e "Materiais a utilizar" (presentes no formulário principal de /dashboard/tasks).

- `src/app/dashboard/calendar/CalendarClient.tsx`:
  - Adicionado `DynamicList` sub-componente (mesmo padrão de `TasksClient.tsx` e `TaskSummaryActions.tsx`)
  - Adicionado estado `ntSafetyRules: string[]` e `ntMaterials: string[]` (reset em `openNewTask`)
  - `handleCreateNewTask` serializa listas válidas como JSON → `safetyRules` + `materialsRequired` no formData
  - Modal agora inclui secção "Regras de segurança" (ShieldAlert) e "Materiais a utilizar" (Package)
  - Imports: `Minus, Package` adicionados; `React` importado explicitamente para tipagem `React.ReactNode`

Fix paralelo já feito nesta sessão: `toYMD` corrigido de UTC (`toISOString().slice(0,10)`) para hora local (`getFullYear/getMonth/getDate`) — eliminava desfasamento de 1 dia no fuso UTC+1.

## Estado final

- `npm run build` → ✅ 23 rotas, sem erros de tipo
- Deploy Vercel produção → ✅ a decorrer
