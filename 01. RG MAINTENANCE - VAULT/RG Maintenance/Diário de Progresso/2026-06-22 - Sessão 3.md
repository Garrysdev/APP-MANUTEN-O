---
data: 2026-06-22
sessao: 3
fase_ativa: "Fase 2 — ecrã de registo + preview visual"
estado: "🟢 Build verde. Registo (empresa+gestor) implementado. Gate A1 pendente"
tags: [diario, sessao, rg-maintenance, registo, firebase, preview]
---

# 📝 Sessão 3 — 2026-06-22 — Ecrã de registo + preview visual da plataforma

[[../00 - Projeto Overview|← Overview RG Maintenance]]

## Ecrã de registo (cria empresa + gestor)

- `src/lib/firebase/data.ts` — `createCompanyWithManager(uid, email, {companyName, userName})`: cria doc
  `companies` (plan free) + doc `users` (role manager) com o uid do Firebase Auth; gera slug
- `src/app/api/auth/register/route.ts` — verifica idToken (Admin), cria empresa+gestor, abre sessão (cookie)
- `src/app/register/page.tsx` — `createUserWithEmailAndPassword` → POST /api/auth/register → /dashboard
- `src/app/login/page.tsx` — link "Criar conta"; `src/middleware.ts` — /register tratado como página de auth
- **Resultado:** dispensa a criação manual de empresa/utilizador no Firestore (passo 4 do SETUP simplifica-se)
- `npx tsc` + `npm run build` → ✅ verdes (11 rotas, incl. `/register` e `/api/auth/register`)

## Preview visual (para o Rui ver o que já foi feito)

- `C:\tmp\rgm-preview\preview.html` + `render.cjs` — render fiel (CSS próprio, sem internet) dos 7 ecrãs
  com dados de exemplo: login, registo, dashboard, equipamentos+modal, tarefas, detalhe/checklist, histórico
- 7 PNGs enviados ao Rui. Nota: é preview com dados de exemplo — falta ligar o Firebase (Gate A1) para
  correr com dados reais

## Próximo

- Plano de Negócio do projeto (pedido do Rui, separado por projeto) — em curso
- Gate A1 (Firebase + deploy) continua pendente do Rui
